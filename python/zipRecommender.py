##1. count customers in zipcode
def countCustomer(l_z):
    #spatial contain calculation
    return l_z


##2. find the zip with max customers
def maxZip(l_z):
    max = 0
    for z in l_z:
        if z.customer > max
            zipCode = z.zipCode
            max = z.customer
        
    return zipCode

##3. zip clustering or zip matching
def zipSim(l_z):
    # make a n by n table of similarity
    # normalize the census table
    # calculate pearson correlation
    l_top = top_k(zipCode, k)
    return l_top



